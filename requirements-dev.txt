-r requirements-train.txt

pytest==9.1.1
httpx==0.28.1
ruff==0.16.8

# MLflow completo (con servidor y UI) sólo en desarrollo: las pruebas de registry usan
# un backend SQLite, que el cliente `-skinny` de la imagen no trae.
mlflow==3.16.1
